{
  "name": "expenseiq-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the ExpenseIQ static web app.",
  "scripts": {
    "mobile:prepare": "powershell -ExecutionPolicy Bypass -File scripts/prepare-capacitor.ps1",
    "mobile:sync": "npm run mobile:prepare && npx cap sync android",
    "mobile:android": "npm run mobile:prepare && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/core": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0"
  }
}
